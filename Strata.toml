[package]
name = "strata-enterprise-core"
version = "0.3.0-alpha"
authors = ["Prabath Kumar"]
edition = "2026"
license = "Apache-2.0"

[compiler]
target = "native"
opt_level = "aggressive"
halt_on_error = true

# No external dependencies. The `hub` registry does not exist yet; entries
# referencing it, and their checksums, were not real and have been removed.
[dependencies]

[dev-dependencies]

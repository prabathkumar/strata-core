#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(dirname "$SCRIPT_DIR")"
exec python3 -c "
import sys
sys.path.insert(0, '$ROOT')
from compiler.parser import main
main()
" "$@"
